{
  "name": "ix-cli",
  "version": "1.0.0",
  "description": "a cli named ix",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": "core/cli/bin/index.js",
  "author": "clelo",
  "private": true,
  "workspaces": [
    "core/*",
    "commands/*",
    "utils/*",
    "models/*"
  ],
  "license": "ISC",
  "dependencies": {
    "lerna": "^4.0.0"
  }
}
